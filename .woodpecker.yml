when:
  - event: push
  - event: pull_request

steps:
  test:
    image: python:3.12-slim
    commands:
      - pip install --upgrade pip
      - pip install -e ".[dev]"
      - ruff check .
      - mypy src
      - pytest -v
