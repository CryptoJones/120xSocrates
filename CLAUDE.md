# Claude Code adapter

This file exists so that Claude Code picks up project context automatically.

**Read `AGENTS.md` first.** Everything you need to know about this project — layout, rules, invariants — is routed from there. The module-level design lives in `docs/ARCHITECTURE.md`.
